<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.what2fix">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/start_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".StartActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Create"
            android:label="Customer Details"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".WorkerActivity"
            android:label="Worker Details" />
        <activity
            android:name=".StatusActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".ChangePasswordActivity"
            android:label="Forgot Password"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="What2Fix"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DetailsCustomerCompleted"
            android:label="Customer Details"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".DetailsCustomerPending"
            android:label="Customer Details"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".DetailsCustomerProgress"
            android:label="Customer Details"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".wDetails"
            android:label="Workers"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".editWorkerDetails"
            android:label="Edit Worker" />
        <activity android:name=".EditAddWorker"></activity>
    </application>

</manifest>